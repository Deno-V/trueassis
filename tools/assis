#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
assis —— trueassis 私人秘书命令行入口。

零第三方依赖（Python 3.8+ 标准库），任何 AI 工具都能直接调用：
    ./tools/assis brief
详细协议见仓库根目录 AGENTS.md。
"""
import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent))

if sys.version_info < (3, 8):
    sys.exit("需要 Python 3.8 或更高版本")

from assislib.cli import main  # noqa: E402

if __name__ == "__main__":
    try:
        sys.exit(main())
    except KeyboardInterrupt:
        sys.exit(130)
